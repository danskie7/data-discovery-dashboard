import Layout from './components/Layout';
import CompanyList from './components/CompanyList';

const Home = () => {
  return (
    <Layout>
      <CompanyList />
    </Layout>
  );
};

export default Home;
